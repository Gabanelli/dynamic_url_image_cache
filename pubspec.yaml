name: dynamic_url_image_cache
description: A image cache widget that works with dynamic urls
version: 0.0.1
homepage: https://github.com/Gabanelli
repository: https://github.com/Gabanelli/dynamic_url_image_cache

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  path_provider: ^1.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
